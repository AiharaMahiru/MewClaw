/**
 * dsh-lark-admin-bundle bundle 包：只有声明（行集在 cordis.patch.yml）。
 * admin 部署组合层：凭证 + host-webserver + knowledge + admin 面。
 * 不挂 dsh-base——admin 进程物理上没有 agents/tools（硬边界强化）。
 */
export const BUNDLE_NAME = "dsh-lark-admin-bundle";
